import { DefaultDateFormatDirective } from './default-date-format.directive';

describe('DefaultDateFormatDirective', () => {
    it('should create an instance', () => {
        const directive = new DefaultDateFormatDirective();
        expect(directive).toBeTruthy();
    });
});
