import { MonthDayFormatDirective } from './month-day-format.directive';

describe('MonthDayFormatDirective', () => {
    it('should create an instance', () => {
        const directive = new MonthDayFormatDirective();
        expect(directive).toBeTruthy();
    });
});
