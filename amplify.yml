version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build_feature
  artifacts:
    baseDirectory: dist/dashboard-material
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
